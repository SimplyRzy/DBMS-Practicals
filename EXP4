CREATE TABLE STUDENT (
RollNo INT,
Name VARCHAR(30),
Marks INT,
City VARCHAR(20)
);

INSERT INTO STUDENT VALUES (101, 'Amit', 85, 'Nagpur');
INSERT INTO STUDENT VALUES (102, 'Neha', 90, 'Pune');
INSERT INTO STUDENT VALUES (103, 'Rahul', 78, 'Mumbai');
INSERT INTO STUDENT VALUES (104, 'Priya', 88, 'Pune');

SELECT COUNT(*) AS Total_Students FROM STUDENT;

SELECT MAX(Marks) AS Highest_Marks FROM STUDENT;

SELECT MIN(Marks) AS Lowest_Marks FROM STUDENT;

SELECT AVG(Marks) AS Average_Marks FROM STUDENT;

SELECT SUM(Marks) AS Total_Marks FROM STUDENT;

SELECT Name FROM STUDENT WHERE City = 'Pune'
UNION
SELECT Name FROM STUDENT WHERE Marks > 85;
